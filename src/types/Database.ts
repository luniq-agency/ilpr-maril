export interface Jobs {
  description: string;
  excerpt: string;
  location: string;
  organization: number;
  publish_date: Date;
  status: string;
  title: string;
}

export interface News {
  category: string;
  slug: string;
  thumbnail: string;
  title: string;
}
